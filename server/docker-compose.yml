services:
  talkia-server:
    build: .
    container_name: talkia-server
    restart: unless-stopped
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-talkia2026}
    networks:
      - nginx-network

networks:
  nginx-network:
    external: true
